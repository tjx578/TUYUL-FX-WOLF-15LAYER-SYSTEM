/**
 * @deprecated CUTOVER-PHASE-9
 * Metadata ownership moved to (control)/news/layout.tsx.
 * This directory has NO sub-pages — safe to delete in next cleanup pass.
 */
import type { PropsWithChildren } from "react";

export default function DeprecatedNewsLayout({ children }: PropsWithChildren) {
  return <>{children}</>;
}
