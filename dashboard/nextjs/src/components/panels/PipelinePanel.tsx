// Intentional re-export: canonical path is @/components/panels/PipelinePanel.
// Other parts of the app may import via @/components/PipelinePanel — both paths resolve here.
export { default } from "@/components/PipelinePanel";
