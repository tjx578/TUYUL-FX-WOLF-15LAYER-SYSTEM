/**
 * ⚠️  DEPRECATED — This file is intentionally empty.
 *
 * Next.js only loads ONE middleware file.  Since this project uses the src/
 * directory layout, the active middleware lives at:
 *
 *   src/middleware.ts
 *
 * This root-level file was previously a dead-code duplicate that never
 * executed, causing confusion.  All auth-guard and auth-injection logic
 * has been consolidated into src/middleware.ts.
 *
 * DO NOT add middleware logic here — it will be silently ignored by Next.js.
 */
