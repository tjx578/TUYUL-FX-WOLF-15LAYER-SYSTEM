name: Incident Runbook Automation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired-privileged-path:
    runs-on: ubuntu-latest
    steps:
      - name: Reject unbound provider commands
        run: |
          echo "::error::Arbitrary provider command execution is retired. Use the reviewed exact-source release procedure."
          exit 1
