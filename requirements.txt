# Core runtime
python-dotenv==1.0.1
PyYAML==6.0.2
pydantic==2.9.2
pydantic-settings==2.5.2
orjson==3.10.7

# Networking / HTTP / WebSocket
requests==2.32.3
httpx==0.27.2
websockets==13.1

# Reliability / Retry / Scheduling
tenacity==9.0.0
APScheduler==3.10.4

# Data / Math
numpy==2.1.1
pandas==2.2.2

# Storage
redis==5.0.8
asyncpg==0.30.0
psycopg[binary]==3.2.4
alembic==1.14.1

# Logging / CLI UX
loguru==0.7.2
rich==13.8.1

# Dashboard Backend (Read-Only API)
fastapi==0.115.0
uvicorn==0.30.6

# Schema validation
jsonschema>=4.0.0

# Testing
pytest==8.3.2
pytest-asyncio==0.24.0

# Linting
ruff==0.8.4

# Production WSGI/ASGI server
gunicorn==23.0.0
