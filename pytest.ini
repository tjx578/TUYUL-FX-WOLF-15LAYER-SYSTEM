[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration tests
    ws: websocket tests
    concurrent: concurrency tests
    smoke: smoke tests for import chain integrity
    reference_performance: absolute performance SLA tests for an approved reference runner with coverage disabled
addopts =
    --strict-markers
    --timeout=30
    --tb=short
    -q
asyncio_mode = auto
