[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash deploy/railway/start_worker.sh services.worker.montecarlo_job"
restartPolicyType = "NEVER"
cronSchedule = "0 1 * * *"

[env]
WOLF15_WORKER_ENTRY = "services.worker.montecarlo_job"
