[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash deploy/railway/start_worker.sh"
restartPolicyType = "NEVER"
cronSchedule = "0 1 * * *"

[env]
WOLF15_WORKER_JOB = "montecarlo"
