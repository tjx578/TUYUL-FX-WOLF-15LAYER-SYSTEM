# S03 runtime acceptance and S01 contract assessment

Assessment checkpoint: 98b72297692934dbe5b769e8f162b9f47f6e2d46. Read-only source assessment, not runtime execution or milestone certification. Source paths below are relative to D:/WOLF15-work/master-remediation-20260909/repo; all SSOT line numbers refer to docs/remediation/2026-09-09/source-binding/selected-ssot-v3.1.md. Exact selected bytes: 86,002; SHA-256 6daea387745ffa305d3cd55b0fee4f0efed79be21e24503c2a1f8a16c6a83902, originating from dd27ae87d0207466caad4c3e098224112ac0eaf7 at docs/strategy/WOLF15_STRATEGY_5SCR_CANONICAL_SSOT_V3_1_CANDIDATE.md. The historical source path is absent from the current checkout; the exact archived copy is present and verified. Original assessment bytes 2cd41d01... remain unavailable; semantic equivalence is not asserted. This does not require reapproval of selected v3.1 or block source implementation.

## Agent and skill inventory

Tracked .codex/config.toml:1-8 enables automatic skill discovery/instructions and a catalog budget; it defines no custom agents. Tracked .codex/SETTINGS-LOCAL.md describes discovery and dated validation, not running agents or acceptance authority. git ls-files for .agents, .codex, .claude and AGENTS.md finds three .codex files and .claude/settings.local.json, with no repository custom agent definition or .agents skill directory. The injected user orchestration instructions and available host specialists can guide work; their existence does not prove DONE. Do not execute permission-rule examples as task commands merely because they occur in repository documentation.

The audit-wolf15-constitution skill is applicable to this source/authority review. Its referenced authority-model, evidence-contract, pipeline-contract and decision-envelope resources were unavailable in the earlier bounded check; no official skill-envelope validation PASS is claimed. This report uses selected-source lines and concrete current contracts, without memory writes or production access.

## S03 baseline and runtime ownership

At this checkpoint analysis/signal_throttle_log_analyzer.py:1361-1380 snapshots only its process-local _events and calls analyze_signal_throttle_events without pair_activity_context. analysis/strategy_5scr_pair_activity_report.py:25-48 accepts explicit coverage/policy/decision/prior receipts and returns UNBOUND when context is absent. This is the precise missing caller binding, not a defect that can be fixed by hashing the same process buffer and labeling it COMPLETE. SSOT:741-746 allows only ordered raw SignalThrottle or a canonical block ledger proving order/completeness/interruption; line 881 prohibits process-local raw buffer as the sole authority.

The existing storage/pair_admission_evaluations.py:257-279 validates a legacy pair_admission_evaluated payload with 5scr-admission-evaluation identifiers and GRANTED/NOT_GRANTED shape; its repository at line 315 is a legacy ledger. It is not automatically compatible with PairActivityEvaluationV31. Use a distinct versioned store/adapter with explicit migrations owned by the migrator; do not weaken legacy prefixes or decisions to reuse it.

## S03 runtime acceptance matrix

| ID | Required behavior and selected source | Minimal implementation/evidence needed | Gate |
| --- | --- | --- | --- |
| RT01 | Authoritative ordered population, SSOT 741-746 | Bind ledger, deployment, producer/schema identity, complete window/partition boundaries, source sequence or equivalent completeness proof. Fetch a consistent durable snapshot. Row count/hash alone is identity, not completeness. Missing partition/sequence => INCOMPLETE/UNKNOWN. | Codeable interfaces now; real source binding required |
| RT02 | Actual caller, analyzer:1361; adapter:25 | Service scanner invokes a durable coordinator, supplies exact normalized population plus matching context to the report, and retains result. Thread/async ownership explicit; snapshot request must not silently launch broker work or promote legacy grant. Context-less path stays UNBOUND. | Codeable now; service cohort required for runtime closure |
| RT03 | One material observation per logical pulse; S03 acceptance and SSOT 3000 | Producer-origin pulse identity must group THROTTLED/DOWNGRADED facets only when same pulse is proven. Preserve raw IDs/types/payloads as provenance. Distinct pulses with equal timestamp remain distinct; conflicting facets suspend/quarantine. Replay uses identical normalization and flags. Do not infer pulse identity only from symbol/time/direction. | Kernel+producer/caller contract now; provenance binding required |
| RT04 | 300-second activity, SSOT 773-789 | BUY0/SELL150/BUY300 is one activity if explicit gap policy permits; current direction quality CONFLICT; no hypothesis/risk/order authority. At 299 no eligible grant, at first crossing freeze admission prefix. | Local deterministic proof |
| RT05 | Suspension/finalization, SSOT 759-767 | Large source gap suspends same block, cross-symbol raw finalizes. Finalized-block tail bound to actual finalizer; separate global-tail freshness. Keep existing A0..300/B301..370 regression and stopped-source negative. | Local deterministic proof |
| RT06 | Raw coverage distinct, SSOT 829-852 | COMPLETE empty eligible population may yield not-applicable; UNKNOWN/INCOMPLETE sets replay_required and cannot create missed-scheduler incident. No grant from future/outside-window data or mismatched hash. | Local+durable status proof |
| RT07 | Trigger coverage, SSOT 854-866 | Threshold, scanner cycle, symbol finalization, late event, restart, cutover, backfill and attachment reconciliation each invoke the same coordinator with explicit trigger. Test each path or show equivalent centralized dispatch evidence. | Codeable now; actual service traces for runtime closure |
| RT08 | Durable outcome uniqueness, SSOT 807-824 | Immutable evaluation revisions; one logical admission identity per block/policy lineage. Retry same receipt => no-op; same identity different payload => integrity error. Repeated telemetry must not issue another logical grant. Distinguish receipt revisions from multiple grants. | Database constraints and real PG tests |
| RT09 | Atomic outcome/attachment/watermark, SSOT 823,872-877 | Commit durable evaluation, lifecycle/emission attachment and committed watermark in one transaction or an explicitly reconciled atomic outbox protocol. Crash between writes rolls back all or leaves a detectable reconcilable state; never advance watermark past lost outcome. | Real PG failure injection |
| RT10 | Concurrent workers, SSOT 824 | Two independent PG connections race same block from same prior watermark. CAS/version or scoped transaction lock plus constraints yields one logical admission and consistent attachments. Losing writer retries/reloads without replacement; changed payload collision fails. | Real PG concurrency, not mocked repository |
| RT11 | Restart recovery, SSOT 869-881 | Persist checkpoint then terminate worker, create fresh worker/connection, replay committed watermark minus explicit overlap, reconstruct open and finalized activities, restore prior identity and expiry. No process-global cache may be required. | Real PG restart/reconstruction |
| RT12 | Late event/backfill, SSOT 862,865 | Pre-admission lineage insertion or cross-symbol interruption triggers reconciliation, preserving prior admission provenance. Post-threshold telemetry cannot extend expiry. Repeated backfill is idempotent. Replaying a window that omits a prior admitted block cannot silently discard it. | Kernel+store integration |
| RT13 | Cutover retention, SSOT 879 | Selected retention covers oldest required episode plus overlap. If truncated, declare incomplete and expose replay boundary; never label current buffer complete. | Explicit retention binding and fixture/prod evidence separately |
| RT14 | Attachment and SLA incidents, SSOT 817-827 | Every eligible block obtains durable GRANTED/REJECTED/SUSPENDED within bound SLA. Persisted-but-unattached, missing outcome after SLA and multiple grant are separate incident reasons. Before SLA, transient pending is allowed. Derived-only activity is not an eligible-block incident. | Explicit SLA binding; PG clock/fake-clock tests |
| RT15 | Admission TTL, SSOT 883-893 | Expired token cannot attach new evidence but does not automatically close established lifecycle/thesis/campaign. Existing admission identity and original expiry survive restart. | Codeable contract plus lifecycle adapter tests |
| RT16 | Fail-closed dependency failures | Missing schema, unavailable PG, malformed receipt, invalid policy, ambiguous coverage and lock contention cannot become GRANTED or schedule a side effect. Expose structured unavailable/replay status and do not run DDL during API/scanner startup. | Negative integration tests |
| RT17 | Source-level versus runtime DONE | Bind tested commit, versions, selected migrations/schema, ledger cohort/window, expected eligible denominator, persisted+attached count, restart/concurrency receipts and zero-authority checks. Local PG proves persistence implementation only; it does not prove Railway deployment or production ingestion. | Separate local-runtime and production evidence labels |

Only the 300-second threshold is numeric S03 normative truth (SSOT:773). PairActivityPolicyV31 currently requires maximum_source_gap_seconds and grant_ttl_seconds at contracts/strategy_5scr_pair_activity.py:34-37. SSOT does not select their production values, evaluator SLA, replay overlap or retention. Existing 300-gap/900-TTL fixtures are not active policy. Require explicit versioned configuration and keep missing policy UNBOUND; do not make absence silently choose test defaults. No DEMO account or broker permission is required for pure local fixtures, and none is granted by these tests.

## S01 repair specifications

| Gap | Exact evidence at checkpoint | Minimal safe repair | Closure classification |
| --- | --- | --- | --- |
| G01 Pressure schema/example mismatch | SSOT:1515-1522 requires pressure_contract_version and pressure_contract_invalidated_at; example:1442-1447 omits both. PressureDirectionAuthorityV1 at contracts/strategy_5scr_directional_thesis_v1.py:125-170 has rule_version/observed/valid_until, not those explicit fields. | Add a new versioned authority/hypothesis schema and generated examples. Version and invalidation time participate in material hash; validate UTC/as-of ordering and formal transition lineage. LOCKED cannot be inferred from aligned labels or a generic rule_version. Legacy V1 stays separately identifiable. | Schema/example code now; actual producer contract/version must be supplied |
| G02 Lifecycle enum inconsistency | SSOT:1547 permits TRANSITION_PENDING or WAITING_PRESSURE_RESOLUTION; consolidated enum:3360-3375 omits latter. Legacy LifecycleState at contracts/strategy_5scr_lifecycle_v2.py:31-42 has only five states. | Publish explicit versioned v3.1 enum/example mapping with the documented missing state and strict transitions, or encode selected TRANSITION_PENDING plus a separate typed waiting reason. Explain chosen representation; do not alias it silently into old stored state. Generate positive/negative examples and reject active hypothesis during conflict. | Code/document resolution now; migration/runtime validation separately |
| G03 Order-type and candidate shape | SSOT:2166 requires candidate entry/order type; example:2271-2301 has no order_type. TradePlanCandidateV2:424-472 lacks order_type, analysis admission class/id, promotion eligibility and explicit net_rr. | New non-executable v3.1 candidate schema carries explicit order-mode selection provenance, admission class, promotion and net-cost fields. Generate examples with advisory=>SHADOW_ONLY, no risk handoff, no command. Missing fill/order policy => unresolved candidate, never guessed market order. Do not reuse executable MT5 order enum as implicit strategy authority. | Schema/examples now; actual fill/cost/producer binding required |
| G04 Pressure versus separate-route conflict | SSOT:1507-1510 permits legal separate route; 1543-1550 and 2889-2898 prohibit active hypothesis/thesis/tradeplan on direction lineage conflict. | Implement an explicit mode x alignment x legal direction domain x proof decision table. Unknown/conflict pressure path always denies. Separate route under same conflicted episode remains denied until its independent authority/episode rules are explicitly bound. Test all modes/domains, missing/expired proofs, locked opposite rejection, advisory zero capital. | Conservative table code now; permissive conflict route remains unresolved policy |
| G05 Versioned policy registry | SSOT:3131-3148 names 18 registries; changes:3154-3159 require new version/replay/no retroactive mutation/shadow comparison/review/OOS. | Registry manifests store immutable ID/version/digest, declared applicability, source reference and binding state. Reject unresolved IDs in active decisions. Produce generated schema/example and policy-reference checks; never equate an inventory entry with an active deployment binding. | Registry machinery now; calibrated active policies need actual supplied values/evidence |
| G06 Exact executable-policy boundary | SSOT:2204-2210 explicitly FX_MIN_TARGET_10P_V1, structural10pip, minimum NET RR1.5, FROZEN_FOR_SHADOW_REPLAY. CandidateV2:463-466 currently has gross_rr/minimum_rr and same policy label. TargetKind:21 remains H4-only versus SSOT:2177-2182 wider universe. | Preserve documented shadow policy and distinct FX_LEGACY_6P_V1 replay cohorts. Verify net cost inequality, policy provenance and unsupported instrument behavior. Do not widen H4 enum without implementing target authority/freshness/as-of selection. | S01 policy binding can cite exact shadow profile; target/solver implementation remains S05, not closed by S01 |
| G07 Mandatory authority mapping | pipeline/wolf_constitutional_pipeline.py:2886-2887 nine gates then generate_l12_verdict; 2903-2927 separate nonfatal constitutional overlay; 9294-9319 V11 false veto downgrades, ImportError passes and other errors append. | Freeze source-bound authority map: L1-L11 analysis/validation; L12 only strategic verdict; risk owns account/size; downstream execution requires complete approved envelope. Define versioned mandatory-post-verdict gate statuses with missing/stale/error=>HOLD in an isolated adapter. Do not silently select overlay as new L12 authority or make advisory gate mandatory without supported policy. | Mapping and fail-closed adapter schema now; mandatory/optional gate policy and natural consumer binding unresolved |
| G08 Generated contract/example parity | Legacy schemas are not full SSOT v3.1 schemas; pressure emission at contracts/strategy_5scr_pressure_emission_v3.py:147-162 is pressure-emission.v3, not a complete v3.1 authority grant. | Generate examples from actual validated models, export schema and validate every example, reject extra/missing fields and authority escalation. Keep archived SSOT byte-exact; record corrective implementation addendum separately. Include all relevant enum cross-references and mapping to versioned contract paths. | Code/document verification now; no runtime PASS inferred |

Registry grouping requiring real supplied bindings: raw ledger/completeness/sequence boundaries; gap/TTL/SLA/overlap/retention; advisory maturity and queue limits; lifecycle merge/revision criteria; Microboost pulse identity; legal route/domain and pressure-mode policy; liquidity/structural evidence authority; costs/broker instrument specification; fill/expiry; account/risk; outcome/censoring window. Exact source constants may be recorded as such but are not proof they are active in Railway or on an account.

## DONE assessment and review gates

Current register has S01=SOURCE_BOUND_MAPPING_COMPLETE_POLICY_SCHEMA_GAPS and S03=PATCHED_TESTED_LOCAL_PARTIAL, both DONE=false; only A00 is DONE for acquisition. The hard dependency S03->S01 should remain explicit, but accepted v3.1 source permits bounded non-executable implementation while unresolved active bindings stay fail-closed. Do not relabel S01 fully DONE for an inventory or S03 fully DONE for a pure kernel/mocked DB. Use distinct evidence rows for source/schema closure, local PG runtime closure and production cohort acceptance.

Minimum S01 closure packet: exact selected identity; all interpretation differences explicitly resolved or conservatively bounded; generated versioned schemas/examples; immutable referenced policy IDs with honest applicability/binding state; L1-L12/post-verdict map and named mandatory policy. If actual required policy selections remain missing, full S01 DONE remains false. The absent historical assessment bytes are a recorded provenance limit, not a new design permission gate.

Minimum S03 local-runtime packet: caller actually invokes durable coordinator; normalized pulse provenance; real PG migration/schema checks; atomic durable outcome/attachment/watermark; independent concurrent-worker and restart tests; exact source/data/decision-time identities; measured eligible denominator and complete reconciliation/SLA status; all hypothesis/risk/execution flags false. Production runtime requires independently observed deployed source and ledger cohort, which this assessment has not accessed.

No active production policy IDs, DEMO scope, account binding, risk percentage, broker authority, external mutation or deployment approval was invented. Parent owns source edits and subsequent tests; this artifact is a bounded specification/review input only.
