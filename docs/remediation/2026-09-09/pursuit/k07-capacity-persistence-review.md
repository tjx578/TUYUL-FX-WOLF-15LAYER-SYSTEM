# K07 account-capacity persistence evidence

The capacity reducer previously produced versioned proposals with no PostgreSQL owner/ledger store. This slice adds an explicitly injected TEST_ONLY CapacityRepositoryV31 and account-level CapacityOwnerFenceV31, separate from the strategy symbol fence. No DSN, runtime activation, active policy or automatic bootstrap is selected.

Initialization requires an epoch-one owner and empty version-zero ledger, complete baseline verification and a post-verifier content check. Account ID is the table primary key, so another executor cannot create a second independently budgeted row for the same account. Repeating the same initialization after state progresses returns current state without resetting its version or balance; different initial content conflicts. The callback must bind actual independent account/baseline truth before any future active usage. Fixture hash verification is not that attestation.

Parent preparation locks account state before invoking the existing candidate repository, which then locks the strategy owner and latest candidate. The actual candidate/target/geometry/sizing/capacity checks run against the loaded ledger. The write compares account, executor, owner ID, epoch, token, version and full predecessor ledger hash. An exact duplicate does not increment the version. Result objects remain tentative and carry no capital or execution authority; the caller still must add campaign, parent leg, FinalSignal and outbox in this same transaction before full Transaction A is implemented.

Owner transfer is explicit, compares expected version and rotates the token while incrementing epoch/version. Other risk and baseline state remains byte-equivalent at the contract level. Prior writers fail after transfer. The migration constrains immutable account/executor/initial identity, sequential version changes and owner-token binding. During epoch changes its trigger rejects any payload changes other than owner_epoch/version; deletes are prohibited. Application CAS and trigger behavior must still be tested on real PostgreSQL. Database grants/role binding remain separate. This new account fence does not cover legacy V1 risk writers; a shared authority decision or guarded runtime selection is required before co-activation.

Refresh and reservation transitions reuse the existing reducers under account lock and persist their result. Initial replay cannot roll a refreshed baseline backwards. A pending broker reconciliation reservation cannot be expired as an unissued one. Bootstrap, writer handover, account balance classification and release proofs remain explicit inputs rather than guessed account settings.

Final run k07-cp-0909-a2 passes 170 tests: 18 new persistence/owner cases and 152 existing candidate/latest handoff/baseline/capacity/CI receipt cases. Exact collection/JUnit identities match, with no errors/failures/skips and unchanged source. The persistence tests use labeled protocol fakes and actual pure reducers; no PostgreSQL durability or race result is inferred. a1 passed 168 before correcting a fixture Decimal type and adding stale/rolled-back handover cases. Runs overlap and are not summed. Ruff/format/diff checks pass.

Eight separate PostgreSQL cases are authored and collected: initialization recovery, concurrent initialization, initialization rollback, owner handover, raw write fencing, parent restart/duplicate, concurrent parent duplicate and parent rollback. These require guarded disposable setup and migration 20260909_05. Graph and 04:05 offline SQL generation pass, but migration application and all eight cases remain NOT_EXECUTED. Seven candidate-history and 63 S03 cases retain their own identities and unexecuted status.

Serial review considered cross-executor account duplication, state rollback, owner transfer, compare-and-swap loss, account-before-symbol ordering and risk retention. Account serialization bounds concurrent writes; the current ledger contains up to 10,000 reservation/history entries and is serialized as one payload. Actual row bloat, lock contention and throughput are not measured. Independent agent review is unavailable. Legacy writer convergence, real database acceptance and full Transaction A remain material gates.

No canonical action closes and milestones remain 0/6. README, canonical register and prior evidence remain unchanged. Next executable source work is immutable campaign/parent-leg/signal/outbox composition with this capacity write in one transaction, while preserving TEST_ONLY and avoiding legacy signal-schema relabeling. No push, merge, deployment, database migration or broker operation occurred.
