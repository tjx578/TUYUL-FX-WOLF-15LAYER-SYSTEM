# Channel B observation window binding

Collector payloads already contain observed_at_utc but prior reconciliation did not validate those timestamps. The parent _broker_snapshot now records UTC start/finish around its subprocess invocation, replacing any child-asserted collection_interval. Each of the five broker payload observations must be timezone-aware and fall within that interval; the interval must begin at or after the requested historical window end. Missing, reversed or pre-window intervals remain NOT_MEASURED.

Expected versus actual: 13 new cases cover all five payloads for invalid/missing/naive/too-early/too-late observations, malformed intervals, valid inclusive boundaries, and parent replacement of child interval assertions. Final bc1 has 74 passes, no skip, exact JUnit identities and source unchanged. Existing Channel B and receipt-integrity tests remain in the same suite. Ruff/format pass on changed files.

Review limits: test subprocess is mocked; no MT5, broker or production database is accessed. This is temporal consistency, not an authenticated clock or collector identity. No cross-host clock-skew allowance is invented; compatibility needs bound runtime evidence. Historical input fixtures now explicitly carry the timestamp fields emitted by the existing collector. Historical reports lacking collection bounds cannot be treated as measured under this contract. The checks do not prove current freshness when a historical report is consumed later, a simultaneous DB/broker snapshot, or full natural DEMO reconciliation. Existing timeout, read-only boundaries and false execution/production readiness fields remain.

No canonical closure; milestones 0/6. README/register preserved, publication/runtime gates HOLD, independent agent review unavailable. Next work: configured collector identity/source binding and a trusted consumer that checks report age/account/window and independently retained digest; do not use heartbeat self-report as broker attestation.
