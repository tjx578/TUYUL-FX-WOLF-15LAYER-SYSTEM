# K05 explicit reference-pattern derivation and verifier composition

Ordered evidence previously verified source content, identity and clocks while leaving every pattern claim external. The new ReferencePatternPolicyV31 explicitly selects a TEST_ONLY adjacent H1 pair and M15 triple, referencing the existing 5scr.directional-thesis.v1 predicates. This is a bounded source-derived reference rule, not selection of an active v3.1 strategy policy or a full V1 scanner.

derive_reference_pattern_v31 requires the supplied policy hash and route to match, exactly two H1 and three M15 candles, adjacent witness windows and an M15 break opening no earlier than H1 confirmation. It computes H1 and M15 directional reference levels from anchor/reference extremes, requires closes beyond those levels, and reuses classify_m15_completion to derive acceptance, failed reclaim or retest. build_reference_pattern_receipts_v31 records hashes of deterministic resolution bodies including scope, policy, level version, source IDs, clocks and derived prices/classification. Content-consistent candles without the claimed price pattern are rejected.

ReferencePatternHandoffVerifierV31 composes these checks with mandatory remaining-domain attestation. It verifies claimed resolution hashes and completion kind, requires literal True from the attestor and checks that attestation did not mutate the handoff. It is injected explicitly into the existing verifier slot; handoff, latest-candidate and transaction fixtures now exercise that composition. No operational entrypoint is registered and no other configured verifier is replaced globally. The caller still owns full handoff digest binding and all remaining authority gates.

The rule evaluates one supplied witness only. It does not select the latest valid witness, scan omitted later candles for invalidation, establish full session coverage or authenticate a market feed. It also does not derive location policy, authorize a counter-pressure thesis, establish admission/hypothesis inheritance, or select active account/risk policy. Those remain explicit dependencies on the producer and independent attestor. Passing fixture attestation has no runtime authority.

Run k05-pat-0909-a1 passes 207 tests without failures/errors/skips: 21 new pattern/verifier cases and 186 existing ordered-proof/handoff/revision/latest/capacity/transaction/V1 thesis cases. Tests derive all three M15 completion kinds in BUY/SELL directions, reject rehashed false patterns and temporal/policy gaps, reject forged receipts, require remaining attestation, detect mutation and reject a false break through the actual risk handoff without changing the ledger. Exact collection/JUnit identity and unchanged source are verified. Ruff, format and diff checks pass. These overlapping tests are not added to previous totals.

Serial review covered rule provenance, explicit policy selection, continuity, hash binding, mutation checks and callback composition. The pattern witness has fixed size; real service latency and database contention remain unmeasured. Independent review is unavailable. PostgreSQL gates and historical inventory identities remain NOT_EXECUTED. Original inputs, README, canonical register and historical evidence remain preserved.

K05 remains partial, no canonical action closes, and milestone count stays 0/6. No push, merge, deployment, migration or broker action occurred. Next executable source work is later-candle liveness/invalidation and complete-window evidence binding for the selected reference proof, alongside inherited admission/hypothesis and operational context/policy bindings. Keep active policy selection and runtime acceptance distinct from this TEST_ONLY derivation.
