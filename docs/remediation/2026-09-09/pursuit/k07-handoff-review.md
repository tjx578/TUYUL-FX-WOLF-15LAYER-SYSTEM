# K07 canonical candidate handoff evidence

The TEST_ONLY risk path previously accepted a sizing request without an explicit selected-v3.1 candidate envelope. This slice adds UUID candidate identity and revision, admission/thesis/context/box/target references, promotion class, geometry and four mandatory strategy proof references. The envelope binds the selected SSOT SHA-256 and an explicitly TEST_ONLY proof policy. Canonical risk eligibility is represented by risk_handoff_allowed=true while final_direction=WAIT, valid_for_execution=false and both final-signal/command flags remain false. Advisory candidates are shadow-only and rejected before risk verification.

The high-level handoff requires separate complete-packet, target-universe and risk-input verifier callbacks. It checks exact plan/revision/thesis, symbol, direction, decision clocks, execution policy and validity window. It reruns nearest valid target selection and net geometry, compares target identity, fixed SL/TP, entry interval/entry and net RR, and recomputes gross RR with exact rational arithmetic before existing sizing and capacity proposal. Reservation metadata retains the full handoff digest. Hashes bind content; they do not create semantic IDs or independent strategy authority. No legacy ID is relabeled as a UUID candidate.

Serial review found that the target-universe callback could mutate its validated copy after the original hash check. Target selection now rehashes after verification. Tests reject deletion of the nearest target and mutation of the candidate RR during callback verification. This protects the local calculation against changed evidence; a callback that merely returns true still does not constitute an independent attestor.

An exact reservation retry can acknowledge the prior proposal after handoff expiry without rerunning strategy verifiers. The existing reducer checks current owner epoch and the immutable request envelope; it does not allocate again or return fresh execution authority. A new reservation cannot use an expired receipt or a reservation deadline beyond that receipt. A future persistent owner must authenticate ledger state and apply candidate/reservation/campaign/leg/FinalSignal/outbox effects in one transaction. Existing lower-level TEST_ONLY reducer entrypoints are intentionally still available and do not enforce a deployed authorization boundary.

Final run k07-handoff-20260909-a2 passes 195 tests: 30 handoff cases and 165 existing target selection, baseline, capacity, sizing and geometry cases. Collection and JUnit identities match exactly, without skips/errors/failures, and source remained unchanged. a1 passed 188 before final coverage additions; overlapping runs are not summed. Ruff, format and diff checks passed. The full source diff and Git/working-byte hashes accompany the receipt.

This is a fixture connection, not a deployed S3-S5 consumer or Transaction A. Mandatory proof references do not implement every individual SSOT object clock, continuity rule, evidence provider, active policy or advisory-to-canonical revision lifecycle. Durable candidate history and fresh re-evaluation before a new canonical revision remain open. Child eligibility, margin, natural command execution and independent broker evidence remain open. Performance uses existing bounded target/ledger collections and linear scans; runtime load behavior has not been measured. Review was serial; independent agent review was unavailable.

No canonical action closes and milestones remain 0/6. The 63 authored S03 PostgreSQL cases remain NOT_EXECUTED and are not part of this 195-test suite. Linux CI, safe Railway branch publication and DEMO binding remain external gates. README, canonical register and prior evidence are preserved. No push, merge, deployment, database or broker action occurred. Next executable work is durable candidate revision/advisory separation and bound child-policy interfaces, while retaining TEST_ONLY until actual policy and runtime evidence exist.
