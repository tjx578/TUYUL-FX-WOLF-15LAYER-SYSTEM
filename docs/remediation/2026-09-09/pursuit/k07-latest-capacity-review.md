# K07 locked latest candidate to capacity preparation

The high-level pure handoff previously accepted a caller-supplied candidate without consulting durable revision history. The repository now provides prepare_parent_in_transaction: it requires an active caller transaction, binds the existing S03 symbol owner fence, locks the latest stored plan revision, matches the expected full revision receipt and request revision, and passes that stored handoff into the existing target/geometry/sizing/capacity checks. Caller-supplied stale candidate content cannot replace the selected stored revision in this path.

Historical append acknowledgement and current risk preparation have different semantics. Exact historical append replay remains valid without moving latest backwards. Current preparation rejects a superseded revision even if its old receipt is intact, and rejects substituting the latest hash while keeping the old revision number. No automatic re-evaluation or advisory promotion occurs. Advisory rejection, expiry, required verifiers and capacity epoch/version gates remain active.

CandidateCapacityPreparationV31 binds the stored revision and capacity proposal to matching plan/revision/thesis scope. It explicitly reports durable_commit=false, capital_reservation_authority=false and execution_authority=false. The method writes no reservation, campaign, leg or outbox and does not claim a commit acknowledgement. It is a component for composition in the same caller-owned transaction, whose account ledger still needs authenticated locking and persistent CAS. The repository is not wired into a deployed runtime. Lower-level TEST_ONLY risk APIs remain callable, so this is not a global execution enforcement boundary.

Final k07-lc-20260909-a2 passes 100 tests: 12 new current-revision integration cases and 88 existing revision/handoff/CI receipt cases. The 12 integration cases use a clearly labeled protocol fake for database behavior while invoking the actual pure risk functions. They do not prove PostgreSQL locks, durability or races. Exact collection/JUnit identities match, no skips/errors/failures occurred and source stayed unchanged. Ruff/format/diff checks pass.

The first run had 99 passes and one CI receipt fixture failure: FileNotFoundError at a deeply nested Windows artifact location. The expected runtime fixture path measured 264 characters; a same-directory path-length write probe reproduced FileNotFoundError errno 2. A shorter unique run label on identical source bytes passed all 100 tests. The failed run is retained with the successful receipt; test counts are not summed. No source workaround weakened the CI evidence gate.

Serial review covered missing history, stale/full receipt mismatch, supersession, advisor containment, transaction presence, owner/capacity checks and false commit claims. Existing symbol-scoped locking remains held while verification/sizing runs; actual contention and capacity are not measured. Independent agent review remains unavailable. Seven candidate-history and 63 S03 PostgreSQL acceptance cases remain NOT_EXECUTED; this slice did not extend or run those inventories.

No canonical action closes and milestones remain 0/6. README, canonical register and prior evidence remain unchanged. Next executable work is persistent account-capacity state and atomic Transaction A composition using this locked candidate preparation, followed by actual PostgreSQL acceptance when the authorized runner is available. Active risk/strategy bindings, child policy, broker reconciliation and safe publication remain required. No push, deployment, database migration or broker action occurred.
