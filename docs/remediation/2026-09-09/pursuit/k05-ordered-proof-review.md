# K05 ordered candle evidence bound to candidate handoff

Selected SSOT section 14.5 requires H1/M15 proof identities, source candle IDs, close clocks, level version and an ordered proof hash. The prior TEST_ONLY handoff carried only an opaque structural proof hash. OrderedProofEvidenceV31 now supplies full candle bodies and the corresponding references, policy/resolution hashes, thesis/context/lifecycle UUID scope and validity window. It reuses the established ClosedCandleAuthorityRefV1 candle format without converting V1 thesis or context IDs.

The existing candle material/evidence hash algorithms were extracted unchanged into contracts/strategy_5scr_candle_identity.py and re-exported from the original analysis module. Both consumers now use the same algorithms. The new evidence contract verifies hashes, source symbol/timeframe, unique ordered candle identities/clocks and proof-close equality to the final source candle. H1 close must precede or equal M15 close, both precede or equal evaluation, and expiry is later. M15 break and completion must reference distinct supplied candles in order, with completion at the proof close.

CandidateHandoffV31 now requires the evidence body and exact hash, matching thesis/lifecycle/context/symbol/direction/route/material-context identity, and an evidence window covering candidate decision and handoff deadline. Its TEST_ONLY discriminator is S3_S5_HANDOFF_TEST_V3. Older opaque V1/V2 handoffs are not relabeled. Historical TEST_ONLY database records require explicit compatibility or re-evaluation review before reuse; no data conversion or migration occurred.

These checks deliberately establish evidence consistency, not pattern truth. H1 structural classification, M15 acceptance/failed-reclaim/retest semantics, market-session coverage, source authenticity, active policy and independent resolution attestation still require the bound producer and external verifier. In particular, a supplied completion-kind label and consistent hash do not prove that pattern happened. No counter-pressure permission, hypothesis/admission authority, risk budget or executable signal follows from this contract alone. Context/policy and live thesis invalidation integration remain open.

Run k05-prf-0909-a1 passed 202 tests without failures/errors/skips. It contains 25 new evidence/binding cases and 177 existing context/handoff/history/capacity/transaction/V1 thesis cases. Negative cases cover future/expired/naive clocks, source close mismatch, duplicate/reversed/wrong-scope candles, hash tampering, missing or reused break/completion IDs, missing level version and candidate scope/hash/expiry mismatch before risk verification. Collection/JUnit identities and unchanged source match. Ruff/format/diff checks pass. This result is not added to overlapping prior suite totals.

Serial review checked dependency direction, compatibility, full input revalidation and bounded linear hash work. Up to 1000 candles per timeframe are accepted; real performance and database contention remain unmeasured. Independent review is unavailable. Existing PostgreSQL acceptance inventories remain unchanged and NOT_EXECUTED. No runtime endpoint, policy, credential, account or broker binding was activated.

K05 remains partial; canonical actions closed: none; milestones: 0/6. Original inputs, README, canonical register and historical evidence remain preserved. Publication/merge remain HOLD. Next executable work is binding actual pattern derivation and resolution evidence under an explicit versioned policy to this handoff, including inherited admission/hypothesis and independent expiry/invalidation. Operational epoch recovery and legacy/new risk-writer convergence remain separate gates.
