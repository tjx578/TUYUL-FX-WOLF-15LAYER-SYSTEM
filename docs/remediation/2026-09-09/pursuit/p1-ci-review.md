# P1 CI capability and real PostgreSQL acceptance

Billing is PAID_REPORTED. GitHub-hosted Linux jobs now execute, so prior billing/no-runner observations remain historical. This campaign reran a prior lint job successfully, then preserved the original 45 PostgreSQL fixture errors. The fixture incorrectly required a loopback server address behind Docker port forwarding. Commit d2487ae0 binds the actual server to the exact inspected container IP while retaining loopback client DSN, destructive opt-in and server disposable markers.

Run 34338772833 tested merge 13d917d80a0f7c2c267dd1d858cf2220fa0c67c9 for PR head d2487ae02cdcbe8322e3ed8cf2b5b88fa0337cd0. Its independent strict receipts report 45 runtime, seven producer/relay and eleven consumer cases passed without skip. The consumer cases include commit then stale relay ACK, identical redelivery, committed inbox deduplication and successor progress; payload quarantine, predecessor waiting, rollback, restart, concurrent duplicates and legacy/stale owner rejection also execute. Transport is authenticated ASGI TestClient plus real PostgreSQL, not deployed HTTPS/TLS acceptance. Bootstrap uses a PostgreSQL superuser; application-role enforcement and operational consumer policy remain open.

Run 34339544343 tested merge dfa6751e7b7a9920bfed270d2b597304d15a8a80 for PR head 81a251a0b96e7785038efc35113cba9fea357ddd. All six inventoried v3.1 PostgreSQL modules pass: 45+7+11+7 candidate+8 capacity+9 Transaction A = 87 distinct cases. These counts are not added to historical local suites. PostgreSQL is 16.15, migration head 20260909_06, fsync and synchronous_commit on. Fresh migration executed in CI; supported upgrade with existing data remains unproved.

CI still fails. The deprecated-import grep interpreted dots as wildcard separators and matched documentation paths; fixed-string matching retains the import ban. Owner-login required an explicit Python fixture interpreter; its 160 dashboard tests now pass, but the production build correctly rejects the job's HTTP origin. A build-only HTTPS .invalid origin patch preserves production validation. Another old fixture tried to mutate a lifecycle symbol, which the new trigger correctly rejects; the updated test proves both database immutability and mismatched incoming evidence rejection. Finally, TEST_ONLY domain tests left an EURUSD owner in the baseline database, fencing unrelated legacy fixtures. The current candidate uses separately cloned, guarded disposable domain databases. No trigger is disabled to make those tests pass.

C01 remains partial due to alternate deployment paths and final gates. C02 has real consumer persistence/fencing evidence but lacks application-role, dedicated process owner and zero-dispatch combined acceptance. C03 has executed Linux CI with identified failures, pending final PASS. C04 has Linux identity/capacity and PostgreSQL/Redis observations; a dedicated bootstrap qualification receipt is pending. C05 remains blocked by unprotected main and disabled ruleset; no administrative setting changed. C06 lacks built-image runtime fault/shutdown acceptance. No milestone is closed by these receipts.

Railway frontend auto-deploy was directly observed disabled; other same-repository services use main or the separate release branch. Draft publication resumed without merging or deploying. The Railway agent unexpectedly persisted its own memory note during a read-only follow-up; no infrastructure mutation was observed, and further agent calls were avoided. Codex memory was not changed. Serial review checked address binding, receipt identity/no-skip enforcement, clone target guards and environment restoration; no independent reviewer was available. README, canonical register, acquisition DONE and historical receipts are preserved.

## C04 closure update

Run 34340383026 produced an explicit clean-checkout bootstrap receipt at tested merge 44940235f7984c6f082931f75f5888a26e5d8cf0 for PR head d9515f8e433f2752aab4258ef380a4a9ef200998. Linux 6.17.0-1022-azure / Python 3.11.16, approximately 15.61 GB available of 16.77 GB visible RAM, PostgreSQL 16.15 disposable markers/migration head, and loopback Redis 7.4.11 PING are measured. C04 is ACTION_DONE for this existing CI runner scope. No VPS was provisioned. All other P1 actions remain open, so milestones remain 0/6. The same run passed dashboard build; complete Python suite was still running when this receipt was captured. Later release-gate commit f560ed67 adds mandatory runner and PostgreSQL step identities; its 63 local gate tests pass and do not authorize deployment.

## Supported upgrade, built API, and complete-suite defects

Candidate 22766647 follows tested c9a01ff3 and 2e07d41f. Each archived run binds 87 PostgreSQL identities with zero skip/error/failure. Run 34342279046 tested merge 864bcfaaaed2ef00b3d639fbece3e487bfaea704 and upgraded a populated TEST_ONLY capacity ledger from 20260909_05 to _06 with identical row hash. Run 34343429530 built the API image and passed full router import, served health 200/readiness 503 with missing dependencies, graceful shutdown, and fatal mandatory-router/bootstrap controls. This does not close complete C06 worker supervision.

Complete suite c9 recorded 10161 tests, 12 failures, 15 errors and 22 skips; 2e07 recorded 10163 tests, 13 failures, 15 errors and 22 skips. Those receipts remain failures. Source 22766647 corrects stale-price quality precedence and currency reason mapping, isolates collection-time dependency mocks and forensic writes, fixes the Linux PowerShell fixture, replaces retired dashboard timer tests with active-viewer freshness cases, and repairs 19 relative doc links. Local p1-fix-a4: 231 passed, zero skip/error/failure, unchanged source. p1-fix-a2 is not closing evidence because a fixture appended to the tracked forensic artifact; generated bytes were backed up outside the repo before exact original restoration. No runtime/production artifact was changed.

C04 remains the only closed P1 action. C01/C02/C03/C05/C06 and all six milestones remain open. Full CI on 22766647 is pending; security scan detections and dependency advisories require triage. No merge, Railway deployment, production migration or broker action was performed.

## Executed full suite and required-task supervision

CI run 34347734680 on candidate 5e3ab2e319d18176e0f1080057522c9fcec36a48 passed all jobs. JUnit records 10,159 tests with zero failure/error/skip; six latency benchmarks execute separately without coverage using the unchanged hard budgets. Native MCP remains a required isolated job. Each archived PostgreSQL run executes the same 87 distinct identities across six modules; the original 45 plus producer/relay and consumer/owner scopes remain separate. Security run 34347015778 passed after Starlette 1.3.1 and exact source/finding-hash fixture review. Previous failures and Windows setup errors are preserved.

Candidate 2e80441413cca767eec5c6d8baf4c643407d1159 connects the API outbox task to readiness and the actual API process entrypoint. Local supervisor/lifespan/owner scope: 114 passed; adjacent auth/heartbeat/regression/release scope: 139 passed. These overlapping suites are not a cumulative count. Built image acceptance adds a real late task fault, served causal 503, nonzero exit, one listener and drain-before-pool evidence; execution on this new candidate is pending at capture. C03 remains open because C01 and final candidate acceptance are required. C04 remains ACTION_DONE for existing disposable CI bootstrap only. C01/C02/C05/C06 and all six milestones remain open. No main merge, Railway deployment, production database or broker action. Review was serial by the author; independent agent review remains unavailable.

## Current candidate API runtime fault acceptance

CI 34348573253 completed successfully on 2e80441413cca767eec5c6d8baf4c643407d1159. The full Python suite executed 10,166 tests with no skip/error/failure; six required latency benchmarks ran separately. The same 87 PostgreSQL identities passed in their six dedicated modules. Security 34348573251 passed. Eight built API cases passed: complete router registry, served missing-dependency readiness, one listener, graceful SIGTERM shutdown, late required-worker loss with causal 503 and exit 1, mandatory router failure, bootstrap failure and required-worker constructor failure. Late-worker cleanup precedes database pool close. The 45-second resistant-task process bound is source only until its fault scenario executes. Other service roles and C01/C02/C05 obligations remain open; this does not close P1 or authorize release.

## Consumer application-role and bounded process acceptance

Source 9fcfd80c058178116db31d498c45546746873ad6 passed CI 34350658499: 10,168 Python tests with zero skip/error/failure, and 88 PostgreSQL identities across seven dedicated modules. This includes the original 45 runtime, seven producer/relay and eleven consumer cases; a separate non-superuser LOGIN case adds no change to their identities. It proves commit/dedup, four denied privilege escalations, legacy unfenced rejection and stale owner rejection on disposable PostgreSQL. Nine built API cases passed; a cancellation-resistant worker yielded causal readiness 503 and exit 1 in 46.116 seconds, without pool closure while the writer lived. Security 34350658549 passed. Fresh provider observations show classic main protection active with strict CI Gate/Security Gate/Docs Gate, administrator enforcement and a fresh required approval. PR 428 is now open for review after an external transition. Historical unprotected/Draft/NOT_EXECUTED receipts remain historical. Current S03 transport is authenticated ASGI plus real disposable PostgreSQL; deployed transport, active policy and DEMO binding remain open. No milestone, main merge or deployment is inferred.

## Disabled API profile and release-governance delta

CI 34352508180 on ae2abc5694e8dae478b9ebc90af3e66b4b17e48f passed 10,168 Python tests with zero skip/error/failure. Ten built API scenarios passed: the added read-only profile reports the outbox DISABLED, starts none of the five background concerns, performs zero writer/bootstrap calls, serves dependency readiness 503 without a required-task failure and shuts down gracefully. The resistant worker case exited 1 in 46.050 seconds without early pool closure. Security 34352508173 failed on a Box finding that was independently recomputed as the first 32 hex characters of a migration source SHA-256 in an archived receipt; its exact review repair still requires a new security run. The new source gate checks CI, Security and Docs workflow identity, main SHA, executed steps, latest run and rerun races. Local p1-release-gates-a2 passed 77 cases without skip; this is fixture proof, not provider release. Docs is now triggered without path filtering to supply its required check for code-only changes. Alternate canary controls and other service-role acceptance remain open.

## Consumer restricted-role repair on e70264ce

Independent review reopened the prior application-role anti-bypass claim: its grants allowed deleting the owner row or reading its current token. Migration 20260909_07 introduces a narrow capability binder and a definer trigger with a fixed safe search path. The disposable application login has no owner-table privileges. CI 34358463245 executed migration/upgrade, 45 runtime, 7 producer/relay, 11 consumer and the expanded restricted-role test without skips. Eight privileged statements and two stale/forged token writes were denied. The combined scenario committed the consumer, ignored the expired relay ACK, deduplicated a new-lease retry and admitted the successor without a second mapping/emission. The full suite failed one stale lock-order assertion (10185 passed, 1 failed): it searched for old inline SQL; the corrected test checks the account lock precedes the binder call. New CI is required. The 142-test local receipt is separate from PostgreSQL evidence. D10 expiry/missing-policy and D11 deployed HTTPS remain partial; these scenario IDs do not close canonical actions. S03 and all six milestones remain open. See evidence/consumer-fence-20260909/review.json and its verified receipt/JUnit package. Existing named-role grants and unmanaged-symbol behavior require explicit deployment review. No operational activation occurred.
