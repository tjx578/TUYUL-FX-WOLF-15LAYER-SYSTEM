[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.ingestor"

[deploy]
startCommand = "python ingest_service.py"
healthcheckPath = "/healthz"
healthcheckPort = 8082
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
