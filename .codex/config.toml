# Project-specific skill fit; installed global packages are unchanged.
# Reviewed source identities and reasons: skill-assessment.json.
# AGENTS.md routes relevant skills without modifying global enablement.
# Keep automatic discovery: no machine-specific skill paths or fixed allow list.
# Skill discovery does not require command auto-allow grants.
# Sandbox, approval policy, providers and authentication remain host-controlled.

[skills]
include_instructions = true
max_context_tokens = 10000
