[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash deploy/railway/start_worker.sh"
restartPolicyType = "NEVER"
cronSchedule = "0 2 * * 0"

[env]
WOLF15_WORKER_ENTRY = "services.worker.regime_recalibration"
